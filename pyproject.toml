[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-accounting-bot"
version = "1.0.0"
description = "Telegram群组记账机器人"
dependencies = [
    "aiogram>=3.0.0"
]

[tool.setuptools]
py-modules = ["bot"]